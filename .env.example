# Optional: move Keeptrail's local database and media out of the default macOS path.
KEEPTRAIL_DATA_DIR=/absolute/path/to/keeptrail-data

# Optional: change the loopback API port. The web server and browser smoke config default to 4317.
KEEPTRAIL_PORT=4317
