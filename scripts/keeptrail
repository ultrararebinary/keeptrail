#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
node_dir="${KEEPTRAIL_NODE_DIR:-${HOME}/.keeptrail/node-v22.23.2-darwin-arm64/bin}"
if [[ -x "${node_dir}/node" ]]; then
  export PATH="${node_dir}:${PATH}"
fi
cd "${repo_dir}"
exec npm "$@"
